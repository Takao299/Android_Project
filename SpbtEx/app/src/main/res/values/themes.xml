<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base1.Theme.SpbtEx" parent="Theme.Material3.DayNight"> <!--Theme.Material3.DayNight.NoActionBar-->
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base2.Theme.SpbtEx" parent="Theme.Material3.DayNight.NoActionBar"></style>

    <style name="dark_ac" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="no_ac" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="Theme.SpbtEx" parent="Base1.Theme.SpbtEx">
        <!-- 二つのボタンを同時押しできないようにする -->
        <item name="android:splitMotionEvents">false</item>
    </style>

    <style name="Theme.SpbtEx.nobar" parent="Base2.Theme.SpbtEx">
        <!-- 二つのボタンを同時押しできないようにする -->
        <item name="android:splitMotionEvents">false</item>
    </style>

    <style name="Theme.SpbtEx.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SpbtEx.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SpbtEx.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>